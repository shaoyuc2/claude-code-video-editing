# 00 — 環境安裝：三個工具，一條管線

裝之前先理解你在裝什麼。這三樣東西各管一段，接起來才是完整的剪輯管線：

```
毛片影片
   │
   ▼
whisper-cli ──► 逐字稿（每個字都有時間戳）
   │
   ▼
你 + Claude Code ──► 看著逐字稿做決策：剪哪裡、留哪裡、字幕怎麼上
   │
   ▼
ffmpeg ──► 實際執行：剪、接、變速、燒字幕、輸出
   │
   ▼
成品 mp4
```

---

## 1. ffmpeg / ffprobe — 動手的那雙手

**用途**：所有「動到影片本體」的操作都是它。剪片段、串接、變速、調音量、銳利化、燒字幕、最終編碼輸出，全部。ffprobe 是它的配套檢查工具，用來查影片的規格（長度、解析度、旋轉資訊、音軌）。

**為什麼是它**：ffmpeg 是命令列工具，Claude 能直接對它下指令。傳統剪輯軟體（Premiere、CapCut）是做給人操作的圖形介面，Claude 進不去；ffmpeg 是做給程式呼叫的介面，而 Claude 在這條管線裡就扮演那個程式。「不用開剪輯軟體」這件事能成立，靠的就是它。

**安裝**（Mac，用 Homebrew）：

```bash
brew install ffmpeg
```

**驗證**：

```bash
ffmpeg -version
```

有印出版本號就好。

---

## 2. whisper-cli — 把聲音變成可以剪的文字

**用途**：把影片裡的語音轉成逐字稿，而且是**每個字都帶自己時間戳**的等級（word-level timestamps）。

**為什麼是它**：這是整套方法的地基。有了逐字時間戳，剪輯就從「在時間軸上拖來拖去找位置」變成「看著文字做決策」——剪空拍＝找兩個字之間隔太久的地方、上字幕＝把文字對回時間戳、找某句話＝直接搜尋。你和 Claude 討論的單位是「哪句話」，換算成「第幾秒到第幾秒」是 Claude 的事。沒有逐字稿，Claude 等於閉著眼睛剪。

另一個理由：它跑在你的電腦上，影片和語音內容都留在本機，處理私人素材或還沒公開的內容不用擔心上傳問題。

**安裝**（Mac）：

```bash
brew install whisper-cpp
```

裝完還要下載一個語音模型檔（建議 `large-v3-turbo`，中英文都準）。模型檔約 1.6GB，下載這步直接請 Claude 幫你：開 Claude Code 跟它說「幫我下載 whisper 的 ggml-large-v3-turbo 模型並告訴我存放路徑」。

**驗證**：拿任何一段音檔跑一次轉錄，有產出文字就通了（這步也可以直接叫 Claude 做）。

---

## 3. Claude Code — 指揮中心

**用途**：你用中文下決策級的指令（「剪掉所有超過半秒的停頓」「整體加速一成」），它負責翻譯成正確的 ffmpeg / whisper 指令、執行、檢查結果、回報給你。

**為什麼需要它**：ffmpeg 的參數又多又難記——光是「加速但不讓聲音變尖」就是一串咒語，剪多段再串接還要處理編碼一致性的細節。自己學等於學一門新語言。Claude 補的就是「你的決策」到「機器指令」中間的翻譯層，而且它會記得這支影片前面做過什麼，錯了能自己debug。

**安裝**：需要 Claude Pro 或 Max 訂閱。照官方指示裝：https://claude.com/claude-code

**驗證**：

```bash
claude
```

能進入對話畫面就緒。

---

## 裝完之後

三個都驗證過，就可以開 `01-心法.md`。如果任何一步卡住，開 Claude Code 把錯誤訊息貼給它，說「我在裝 XXX 時遇到這個錯誤」，它會帶你排。這本身就是這套工作方式的第一課：卡住的事丟給它，你只管方向。
